[package]
name = "passage_identity"
version = "0.1.0"
description = "Passage Protocol identity registry (KYC attestations)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "passage_identity"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
